package org.teww.tew.core

// Placeholder so this module builds. Real content (API client, auth
// session, Media3 playback session, repositories) lands per
// IMPLEMENTATION.md's build order — after the TalkBack spike.
internal object Core
